// Realtime business logic is split across:
// - presence.ts — connection tracking and message routing
// - callManager.ts — call lifecycle (initiate, answer, decline, hangup, relay)
// - websocket.ts — WebSocket handler and event dispatch

export {};
